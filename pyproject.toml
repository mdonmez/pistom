[project]
name = "pistom"
version = "0.1.0"
description = "Piston API MCP server to enable LLM to run Python code."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.10.1",
]


[project.scripts]
pistom = "pistom.server:main"